using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RealFinalButtonScript : MonoBehaviour
{
    [SerializeField] private NotebookSystem notebook;
    private void Update()
    {
       /* if (notebook.tapes.Count == 3 && notebook.notes.Count == 3)
        {
          
        }*/
    }
}
